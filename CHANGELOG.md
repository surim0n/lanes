# Changelog

## 0.1.0

Initial release: `lanes` script (run, doctor, status, mode, reviewed), routine and escalate lanes driven by the Codex CLI, worktree mode with patches, hook-enforced architect mode, read-only reviewer, Stop-hook review gate, `lanes.conf` as the single place model names live, tests on macOS and Linux.
