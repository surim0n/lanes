---
description: Show architect mode, the review gate, and which models the lanes resolve to.
allowed-tools: Bash(lanes:*), Bash(${CLAUDE_PLUGIN_ROOT}/bin/lanes:*)
---
!`"${CLAUDE_PLUGIN_ROOT}/bin/lanes" status`

Show the user the lines above. If no output appears above, run `lanes status` yourself.
